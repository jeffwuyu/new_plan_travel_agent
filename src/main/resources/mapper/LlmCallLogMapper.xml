<?xml version="1.0" encoding="UTF-8"?>
<!-- 中文注释：MyBatis 映射文件，负责 Llm Call Log 相关 SQL 与字段映射定义。 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travelagent.mapper.LlmCallLogMapper">

    <insert id="insert" parameterType="LlmCallLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO llm_call_logs (task_id, user_id, call_type, model,
                                   prompt_tokens, completion_tokens, total_tokens,
                                   latency_ms, status, idempotency_key, created_at)
        VALUES (#{taskId}, #{userId}, #{callType}, #{model},
                #{promptTokens}, #{completionTokens}, #{totalTokens},
                #{latencyMs}, #{status}, #{idempotencyKey}, NOW())
    </insert>

    <select id="sumTokensByUserAndPeriod" resultType="int">
        SELECT COALESCE(SUM(total_tokens), 0)
        FROM llm_call_logs
        WHERE user_id = #{userId}
          AND status = 'success'
          AND created_at BETWEEN #{startDate} AND #{endDate}
    </select>

</mapper>
